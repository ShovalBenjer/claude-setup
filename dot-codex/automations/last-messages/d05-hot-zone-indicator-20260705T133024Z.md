Done. Wrote the scheduled report to [HOT_ZONES_2026-07-05.md](/home/shovalbe/.claude/docs/HOT_ZONES_2026-07-05.md).

Result: scanned 18 active repo/worktree surfaces, found `0` files meeting the hot-zone threshold, so no top-3 ADO tasks were proposed. The report also records the current blocker: `/home/shovalbe/.claude/bin/work-item.sh` only supports PR comments, not the requested `create --type Task` interface.

Verification included local `git log` scans, report readback, and `wc -l`; the report is 134 lines and is currently an untracked file.

