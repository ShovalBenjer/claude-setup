Completed the sweep.

Report written: [PLATFORM_HYGIENE_2026-07-05.md](/home/shovalbe/.claude/docs/PLATFORM_HYGIENE_2026-07-05.md)

Created Hive beads:
- `#153` P1: Wire Claude Bash RTK guard for parity
- `#154` P2: Make documented Obscura CDP smoke-testable

Key findings captured in the report: Claude Bash is still not wired to the RTK guard, Obscura is documented but stopped, `axia-seekapa-cs-agents` is missing Hive parity manifests, PST DB uses `pst_messages` rather than `emails`, and Codex config is `gpt-5.5` but interactive reasoning is set to `high` while automation defaults to `medium`.

Verification included required section checks, report line count, and a redaction scan for obvious credential/email-body markers.

