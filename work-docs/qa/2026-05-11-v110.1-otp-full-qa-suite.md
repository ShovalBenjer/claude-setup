# v110.1 OTP — Full QA Suite

**Goal:** sign-off testing for the complete v110.1 OTP flow before removing the `SMTP_TEST_REDIRECT_EMAIL` backdoor and exposing the flow to real customers.

**Setup state (must be true before starting):**
- PR 262 hotfix merged + CI deployed (~5-10 min after merge)
- `SEEKAPA_VERIFY_ENABLED=true`
- `SMTP_TEST_REDIRECT_EMAIL=shoval.be@i-sdd.com` (or another inbox you control)
- Inbox 4 is testing-only (no real customer impact during QA)

**Test customer:** `messaoudi.yahia13@gmail.com`
- `compliance_status` = `ADVANCED VERIFIED` ✓ unlocks
- `language` = `ara` → all post-CRM replies in Arabic
- `balance` = $6.18, `equity` = $6.18, `pnl` = null (renders as "n/a")
- `last_deposit_usd` = $294.92 (only relevant for v110 deposit-factor, not v110.1 OTP — kept for context)

**3 windows to keep open during QA:**
1. Telegram chat with ittester on inbox 4
2. Your inbox `shoval.be@i-sdd.com` — for OTP emails
3. Log Analytics workspace `ef6e6783-8c3c-486f-a10a-b9bbee73032b` — `AppTraces | where TimeGenerated > ago(30m) | where Message startswith "[VERIFY]"`

---

## Section A — Happy Path (Bug 1 regression suite)

### A1. EN trigger / AR CRM customer (cross-language mid-flow switch)

| Step | YOU SEND | BOT EXPECT | LOG EXPECT |
|---|---|---|---|
| A1.1 | `Hey` | `Hi, I'm Nisreen. How can I help?` (bare greeting, case 9) | — |
| A1.2 | `Whats my balance` | `I can verify your identity to answer that. Reply with the email registered with Seekapa, or 'cancel' to keep this generic.` (EN — trigger lang) | `[VERIFY] flow start: state_key=<id> lang=en` |
| A1.3 | `messaoudi.yahia13@gmail.com` | AR: `أرسلت رمزاً مكوناً من 6 أرقام إلى بريدك المسجل. الصقه هنا عند وصوله. ينتهي خلال 5 دقائق. اكتب 'resend' لرمز جديد أو 'cancel' للإلغاء.` (post-CRM, switches to ara) | `[VERIFY] FACTOR1 crm_lookup status=200`, `[VERIFY] OTP REDIRECTED ... claimed=me***@gmail.com actual=sh***@i-sdd.com`, `[VERIFY] OTP sent lang=ar brand=seekapa` |
| A1.4 | (check your inbox) | Email arrives ~10s: subject `رمز التحقق من Seekapa: NNNNNN`, body in AR, from `sender@mail.mailseekapa.com` | — |
| A1.5 | Paste the 6-digit code | AR: `تم التحقق. الرصيد 6.18 دولار · حقوق الملكية 6.18 دولار · الربح/الخسارة العائمة n/a دولار.` | `[VERIFY] OTP PASS: state_key=<id> attempts=1` |

**Pass criteria:** A1.5 reveals real CRM balance ($6.18) in Arabic. State cleared after PASS.

### A2. AR trigger / AR CRM customer (consistent AR throughout)

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| A2.1 | `ما هو رصيدي؟` | AR ASK_EMAIL: `للوصول إلى تفاصيل حسابك، أرسل البريد الإلكتروني المسجل لدى Seekapa، أو 'cancel' للإلغاء.` |
| A2.2 | `messaoudi.yahia13@gmail.com` | Same AR ASK_OTP_SENT as A1.3 |
| A2.3 | Paste code from inbox | AR VERIFIED, same as A1.5 |

**Pass criteria:** trigger language preserved through entire flow.

### A3. ES trigger fallback (no ES customer in CRM — degrades to Variant A)

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| A3.1 | `¿cuál es mi saldo?` | ES ASK_EMAIL: `Para verificar tu identidad, envía el email registrado en Seekapa, o 'cancel' para cancelar.` |
| A3.2 | `nobody.es@example.com` (NOT_FOUND email) | EN Variant A (trigger lang for NOT_FOUND): `I've passed this to our support team. Response may take longer...` |

**Pass criteria:** NOT_FOUND email = NO Brevo send, NO email in your inbox. Bot degrades cleanly.

### A4. PT trigger + AR customer (mixed)

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| A4.1 | `qual é o meu saldo?` | PT ASK_EMAIL: `Para verificar sua identidade, envie o email registrado na Seekapa, ou 'cancel' para cancelar.` |
| A4.2 | `messaoudi.yahia13@gmail.com` (ARA customer) | AR ASK_OTP_SENT (CRM lang wins post-lookup) |
| A4.3 | Paste code | AR VERIFIED |

**Pass criteria:** language switches PT → AR after CRM hit. Email body in AR.

---

## Section B — Bug 1 Regression (phantom must NOT fire)

### B1. First-turn intent in any language → straight to ASK_EMAIL (no phantom)

For each row, send a fresh trigger (resolve conv between to ensure no state leak). Bot must reply with ASK_EMAIL, NEVER "Would you like me to proceed with verification?"

| # | YOU SEND | BOT MUST REPLY |
|---|---|---|
| B1.1 | `my balance please` | EN ASK_EMAIL |
| B1.2 | `check my account funds` | EN ASK_EMAIL |
| B1.3 | `ما هو حسابي؟` | AR ASK_EMAIL |
| B1.4 | `mi saldo, por favor` | ES ASK_EMAIL |
| B1.5 | `minha conta foi bloqueada` | PT ASK_EMAIL |

**Fail signal:** any "Would you like me to proceed with verification?" reply. That's pre-PR-262 phantom behavior.

---

## Section C — Bug 2 Regression (EMAIL-state intent retrigger)

### C1. Re-trigger restarts flow (doesn't return INVALID_EMAIL)

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| C1.1 | `whats my balance` | EN ASK_EMAIL |
| C1.2 | `whats my balance` (re-trigger instead of email) | EN ASK_EMAIL **again** (refreshed) — must NOT say "doesn't look like an email" |
| C1.3 | Verify state still EMAIL: `messaoudi.yahia13@gmail.com` | AR ASK_OTP_SENT (flow continues) |

**Fail signal:** C1.2 returning "That doesn't look like an email" → Fix B not deployed.

### C2. Non-intent garbage still returns INVALID_EMAIL (negative test)

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| C2.1 | `whats my balance` | EN ASK_EMAIL |
| C2.2 | `lalalala garbage 123` (NO intent match) | EN: `That doesn't look like an email. Try again, or 'cancel'.` |
| C2.3 | `messaoudi.yahia13@gmail.com` | AR ASK_OTP_SENT |

**Pass criteria:** the existing INVALID_EMAIL path still works for non-intent messages.

### C3. OTP-state intent re-trigger does NOT restart (negative test)

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| C3.1 | Get to OTP state (trigger + valid email) | AR ASK_OTP_SENT |
| C3.2 | `whats my balance` (intent in OTP state) | AR: `هذا لا يبدو رمزاً مكوناً من 6 أرقام. حاول مرة أخرى، أو 'cancel'.` (INVALID_OTP, NOT restart) |
| C3.3 | Paste correct code from inbox | AR VERIFIED |

**Pass criteria:** OTP state preserved — Fix B scope limited to EMAIL only.

---

## Section D — Bug 3 Regression (conversation-resolve cleanup)

### D1. Resolve mid-flow → next message starts clean

Need Chatwoot UI access for this — click "Mark resolved" in the conversation header.

| Step | YOU DO / SEND | BOT EXPECT |
|---|---|---|
| D1.1 | Send `whats my balance` | EN ASK_EMAIL |
| D1.2 | Click "Mark resolved" in Chatwoot UI | (no bot reply) |
| D1.3 | Send `whats my balance` | EN ASK_EMAIL **fresh** — must NOT say "doesn't look like an email" |

**Log expect on D1.2:** `[WEBHOOK] VERIFY: cleared state on conversation_resolved state_key=<id>`

**Fail signal:** D1.3 returning INVALID_EMAIL → Fix C not deployed.

---

## Section E — Wrong OTP / Cap

### E1. 3-attempt cap

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| E1.1 | `whats my balance` + `messaoudi.yahia13@gmail.com` | AR ASK_OTP_SENT (email arrives in your inbox with REAL code) |
| E1.2 | `000000` (wrong) | AR: `هذا الرمز لا يطابق. حاول مرة أخرى، أو 'cancel'.` (1/3 used) |
| E1.3 | `111111` (wrong) | Same AR retry (2/3 used) |
| E1.4 | `222222` (wrong) | AR Variant A: `لقد قمت بتحويل طلبك إلى فريق الدعم...` (cap hit, session cleared) |
| E1.5 | `messaoudi.yahia13@gmail.com` (try to re-send email without re-triggering) | LLM responds (no flow state — session was cleared on E1.4) |

**Log expect on E1.4:** `[VERIFY] OTP EXHAUSTED: state_key=<id>`

### E2. Invalid format doesn't burn attempt

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| E2.1 | (in OTP state) `abc` | AR: `هذا لا يبدو رمزاً مكوناً من 6 أرقام...` (INVALID, no attempt burned) |
| E2.2 | `12345` (5 digits) | Same INVALID_OTP_REPLY |
| E2.3 | `1234567` (7 digits) | Same INVALID_OTP_REPLY |
| E2.4 | Paste real 6-digit code | AR VERIFIED (0 of 3 attempts consumed by E2.1-E2.3) |

---

## Section F — Resend

### F1. Basic resend

| Step | YOU SEND | BOT EXPECT | INBOX |
|---|---|---|---|
| F1.1 | trigger + email | AR ASK_OTP_SENT | 1st email |
| F1.2 | `resend` | AR: `أرسلت رمزاً جديداً. تحقق من بريدك.` | 2nd email with **new** code |
| F1.3 | Paste OLD code (1st email's) | AR: `هذا الرمز لا يطابق...` (old code invalidated) |
| F1.4 | Paste NEW code (2nd email's) | AR VERIFIED |

### F2. Resend in 3 supported languages (AR / ES / PT)

For each, get to OTP state and send the localized command:

| Lang | Command | Expected reply |
|---|---|---|
| AR | `إعادة إرسال` | `أرسلت رمزاً جديداً. تحقق من بريدك.` |
| ES | `reenviar` | `Envié un nuevo código. Revisa tu bandeja de entrada.` |
| PT | `reenviar` | `Enviei um novo código. Verifique sua caixa de entrada.` |

### F3. Resend rate-limit (3 per email per hour cap)

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| F3.1 | trigger + valid email | AR ASK_OTP_SENT (1st send) |
| F3.2 | `resend` | AR OTP_RESENT (2nd send) |
| F3.3 | `resend` | AR OTP_RESENT (3rd send) |
| F3.4 | `resend` | AR: `طلبات كثيرة جداً لهذا البريد. انتظر ساعة أو 'cancel' وحاول لاحقاً.` (CAPPED) |

**Pass criteria:** 4th attempt blocked.

---

## Section G — Cancel

### G1. Cancel at EMAIL state

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| G1.1 | `whats my balance` | EN ASK_EMAIL |
| G1.2 | `cancel` | EN: `OK — anything else?` (trigger lang for cancel) |
| G1.3 | `hi` | Bare greeting (state cleared, fresh session) |

### G2. Cancel at OTP state

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| G2.1 | trigger + valid email | AR ASK_OTP_SENT |
| G2.2 | `cancel` | AR: `حسناً، أي شيء آخر؟` (CRM lang) |
| G2.3 | `hi` | Bare greeting |

### G3. Cancel-equivalents (stop / nevermind / skip)

| YOU SEND | BOT EXPECT |
|---|---|
| `stop` | Same as cancel |
| `nevermind` | Same as cancel |
| `skip` | Same as cancel |

---

## Section H — NOT_FOUND / NOT_VERIFIED / SERVICE_ERROR

### H1. NOT_FOUND (email valid format, not in CRM)

| Step | YOU SEND | BOT EXPECT | BREVO |
|---|---|---|---|
| H1.1 | `whats my balance` | EN ASK_EMAIL | — |
| H1.2 | `nobody.real@example.com` | EN Variant A (degrade) | **NO send** (CRM lookup before OTP — no oracle leak) |

**Verify Brevo dashboard:** no transactional log entry for `nobody.real@example.com`.

### H2. NOT_VERIFIED (real customer email, compliance != VERIFIED/ADVANCED VERIFIED/FULLY VERIFIED)

If you can find a CRM customer with `compliance_status="PARTIALLY VERIFIED"` or similar (e.g. via earlier probes — `ana@gmail.com` was NOT VERIFIED lang=por):

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| H2.1 | `whats my balance` | EN ASK_EMAIL |
| H2.2 | `ana@gmail.com` (NOT VERIFIED, lang=por) | PT Variant A (CRM lang for NOT_VERIFIED): `Encaminhei isso à nossa equipe de suporte...` |

### H3. INVALID_EMAIL format (retry preserved)

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| H3.1 | `whats my balance` | EN ASK_EMAIL |
| H3.2 | `not-an-email` | EN: `That doesn't look like an email. Try again, or 'cancel'.` |
| H3.3 | `messaoudi.yahia13@gmail.com` | AR ASK_OTP_SENT (state preserved through invalid email) |

---

## Section I — TTL Expiry

### I1. EMAIL state expires after 5 min

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| I1.1 | `whats my balance` | EN ASK_EMAIL |
| I1.2 | (wait 6+ minutes — idle) | (no message) |
| I1.3 | `messaoudi.yahia13@gmail.com` | (LLM falls through — session expired, no longer in flow) |

**Pass criteria:** I1.3 doesn't get treated as a flow-EMAIL response. Bot says something LLM-generated, not ASK_OTP_SENT.

### I2. OTP state expires after 5 min

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| I2.1 | trigger + valid email | AR ASK_OTP_SENT (email arrives in inbox) |
| I2.2 | (wait 6+ minutes — idle) | (no message) |
| I2.3 | Paste the OTP code | LLM response (session gone — code is meaningless) |

---

## Section J — Replay (single-use code)

| Step | YOU SEND | BOT EXPECT |
|---|---|---|
| J1.1 | Full happy path through to AR VERIFIED | session cleared |
| J1.2 | Paste the SAME code again later | LLM response (session no longer exists) |

**Pass criteria:** code is single-use; matched code clears session; replay attack neutralized.

---

## Section K — Multi-customer isolation

If you have access to 2 different Telegram contacts (or your test inbox + a colleague's), verify state isolation:

| Step | Contact A | Contact B | Expectations |
|---|---|---|---|
| K1.1 | `whats my balance` → EN ASK_EMAIL | — | A's state set to EMAIL |
| K1.2 | — | `whats my balance` → EN ASK_EMAIL | B's state set independently |
| K1.3 | `messaoudi.yahia13@gmail.com` → ASK_OTP_SENT | — | A advances, B unaffected |
| K1.4 | — | Paste random code | B still in EMAIL state — should get INVALID_EMAIL (since B never entered email)... wait, no — INVALID_EMAIL only fires for non-email content reaching `verify_first_factor`. If B types random code, it goes through verify_first_factor → not a valid email format → INVALID_EMAIL ✓ |

**Note:** if only one contact available, skip Section K (state isolation is well-covered by 53 unit tests).

---

## Section L — Brevo dashboard verification

After completing Sections A–K, open Brevo → Statistics → Transactional.

**Expected counts (rough):**
- ~10-15 successful sends to `shoval.be@i-sdd.com` (your inbox)
- 0 sends to `messaoudi.yahia13@gmail.com` (real customer — never touched)
- 0 sends to `nobody.real@example.com` (NOT_FOUND, no send)
- 0 sends to `ana@gmail.com` (NOT_VERIFIED, no send)

**Sender breakdown:**
- All sends from `sender@mail.mailseekapa.com` (since `broker=seekapa` is default)
- 0 from `sender@axiaemail.com` (no Axia path in inbox 4)

---

## Section M — AppTraces verification

After QA, run this kusto in Log Analytics:

```kusto
AppTraces
| where TimeGenerated > ago(60m)
| where _ResourceId contains "func-cs-agents-dev"
| where Message startswith "[VERIFY]"
| project TimeGenerated, Message
| order by TimeGenerated asc
```

**Expected log patterns:**

| Pattern | Count expected |
|---|---|
| `[VERIFY] flow start` | one per "trigger" message (Sections A, C, D, E, F, G, H, I, J + B's 5) |
| `[VERIFY] FACTOR1 crm_lookup status=200` | one per valid email |
| `[VERIFY] FACTOR1 status=404` or `errors_array_present` | for NOT_FOUND tests |
| `[VERIFY] OTP REDIRECTED (test mode): claimed=me***@gmail.com actual=sh***@i-sdd.com` | one per OTP send during QA (proves backdoor active) |
| `[VERIFY] OTP sent: to=me***@gmail.com lang=ar brand=seekapa` | one per successful Brevo send |
| `[VERIFY] OTP PASS: state_key=<id> attempts=N` | one per successful verify |
| `[VERIFY] OTP EXHAUSTED: state_key=<id>` | one for E1.4 |
| `[VERIFY] EMAIL-state intent retrigger: state_key=<id> — refreshing` | one for C1.2 |
| `[WEBHOOK] VERIFY: cleared state on conversation_resolved` | one for D1.2 |

**PII redaction sanity check:**

```kusto
AppTraces
| where TimeGenerated > ago(60m)
| where Message contains "messaoudi" or Message contains "@gmail.com" or Message contains "@i-sdd.com"
| project TimeGenerated, Message
| take 10
```

**Expect:** ZERO raw email addresses. Every email reference should be `me***@gmail.com`, `sh***@i-sdd.com`, `<email-redacted>`, etc.

---

## Pass criteria for full sign-off

Minimum to call v110.1 production-ready:

- [ ] A1 + A2 happy paths complete (EN-to-AR + AR-to-AR)
- [ ] B1.1-B1.5 all 5 languages return ASK_EMAIL (phantom is gone)
- [ ] C1 EMAIL retrigger restarts cleanly
- [ ] C2 non-intent garbage still returns INVALID_EMAIL
- [ ] C3 OTP state intent stays as INVALID_OTP (not restarted)
- [ ] D1 conversation-resolve clears state
- [ ] E1 3-attempt cap triggers Variant A
- [ ] F1 resend works + invalidates old code
- [ ] F3 4th resend in an hour is rate-limited
- [ ] G1+G2 cancel works at both states
- [ ] H1 NOT_FOUND email = no Brevo send
- [ ] H3 INVALID_EMAIL retry preserves session
- [ ] I1+I2 TTL expiry clears state
- [ ] J replay attack neutralized (code single-use)
- [ ] L Brevo dashboard shows expected pattern
- [ ] M AppTraces show all expected log lines + ZERO raw email leaks

---

## After QA passes — cleanup sequence

```bash
# 1. UNSET the test redirect (CRITICAL — leaving it set leaks all real customer OTPs to your inbox)
az functionapp config appsettings set \
  -g AZAI_group -n func-cs-agents-dev \
  --settings "SMTP_TEST_REDIRECT_EMAIL="

# 2. Verify it's empty
az functionapp config appsettings list --name func-cs-agents-dev --resource-group AZAI_group \
  --query "[?name=='SMTP_TEST_REDIRECT_EMAIL'].value" -o tsv
# Expect: empty output

# 3. Keep SEEKAPA_VERIFY_ENABLED=true (or flip it off if not ready for real-customer rollout)
az functionapp config appsettings list --name func-cs-agents-dev --resource-group AZAI_group \
  --query "[?name=='SEEKAPA_VERIFY_ENABLED'].value" -o tsv
# Expect: true (or false if rolling back)
```

**Function App restarts** automatically on appsettings change (~30s).

After the unset, the AppTraces should NEVER show `[VERIFY] OTP REDIRECTED` again. That's the proof the backdoor is off.

---

## Rollback in 30s if anything breaks

```bash
az functionapp config appsettings set \
  -g AZAI_group -n func-cs-agents-dev \
  --settings "SEEKAPA_VERIFY_ENABLED=false"
```

Function App restarts. Verifier inactive. v109 prompt-side identity-guard takes over (the deterministic pre_classify path now restored since flag is off). No customer-data exposure during rollback.
