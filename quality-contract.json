{
  "project": "claude-setup",
  "created": "2026-07-25",
  "_rationale": "docs/QUALITY-CONTRACT.md. Notes below hold no measured status on purpose: for status run `python tools/gate/gate.py run --project .`",
  "app": {
    "start": null,
    "url": "http://localhost:3000",
    "routes": [],
    "_note": "unused; kept in the shape the gate expects so a future served component has somewhere to go"
  },
  "domains": {
    "build": {
      "required": true,
      "cmd": "cd intent-control-plane && uv sync --frozen",
      "_note": "the only subproject with an installable manifest; tools/ has none and imports stdlib only"
    },
    "unit": {
      "required": true,
      "timeout": 900,
      "cmd": "python -m pytest tests/ -q && python tools/gate/gate.py selftest && python tools/review/panel.py selftest && cd intent-control-plane && uv run pytest -q",
      "_note": "every automated-test entrypoint in the repo, including both oracles' own selftests",
      "_timeout_note": "Raised 300 -> 900 on 2026-07-31 with the measurement rather than by feel. The 300s budget was set when the root suite was 70 tests in about 47s. It is now 331 tests in 182s, and this domain chains FOUR commands: the root suite, the gate selftest, the panel selftest, and the intent-control-plane suite at about 50s. Total is roughly 250s on an idle machine and it timed out at 300s three times on 2026-07-31 under concurrent load, which produced a FAIL that named no failing test and sent two diagnoses down the wrong path. The honest alternative is splitting this into unit and subproject domains so a slow subproject cannot mask a fast regression; that is a contract shape change and is deferred to a ticket rather than done inside a timeout bump. Falsifier: if the suite passes 600s this number is hiding growth again and the split is overdue."
    },
    "types": {
      "required": true,
      "cmd": "python -m compileall -q -x \"\\\\.venv|venv\" . && cd intent-control-plane && uv run ruff check . && uv run mypy",
      "_note": "compileall is the documented fallback where no root lint config exists; ruff and mypy are configured inside intent-control-plane"
    },
    "e2e": {
      "required": true,
      "na_reason": "claude-setup serves no application of its own. tools/e2e/flow.py and tools/browser/cdp.py drive other repositories' running UIs from whichever contract is being evaluated; every .html in this tree is a static export, not a route. See docs/QUALITY-CONTRACT.md."
    },
    "a11y_ux": {
      "required": true,
      "na_reason": "Reads the e2e report at a stricter threshold, and there is no e2e report because there is no served app. If any part of this repo grows a UI, both this and e2e move to a real cmd rather than staying NA by default."
    },
    "security": {
      "required": true,
      "builtin": "secret_scan",
      "_note": "known blind spot is documented beside the patterns in gate.py, not here"
    },
    "docs": {
      "required": true,
      "builtin": "docs_touched",
      "paths": [
        "README.md",
        "docs/",
        "CHANGELOG.md",
        "TODO.md"
      ],
      "_note": "TODO.md is in the list because this repo describes changes there and in docs/analysis/, not in CHANGELOG.md"
    },
    "pipeline": {
      "required": true,
      "builtin": "ci_runs_gate",
      "_note": "local-green and CI-green have to be about the same checks"
    },
    "review": {
      "required": true,
      "artifact": "state/reviews/${SHA}.json",
      "cmd": "python tools/review/panel.py run --project .",
      "_note": "artifact is matched by commit sha, not by tree, so one written against a dirty tree stays current for that commit"
    },
    "perf": {
      "required": false,
      "na_reason": "No performance budget is declared for claude-setup. Every budget-shaped string in the tree is either another project's historical Lighthouse evidence or code that reports p95 without enforcing a threshold. Nobody has set a number to measure against."
    },
    "codemap": {
      "required": true,
      "builtin": "dir_map",
      "_note": "every tracked directory states a purpose, and docs/CODEBASE-MAP.md matches the tree",
      "_waiver_history": "A waiver until 2026-08-06 was added and REMOVED the same hour, unexpired. It excused 5 registry rows naming research-papers/docs-shoval subdirectories that another session had staged for deletion, and its stated reason was that removing the rows would finish half of somebody else's uncommitted change. Then `python tools/map/codemap.py write` was run to register a new directory and it removed those 5 rows as a side effect, which is exactly what the waiver said not to do. The pre-run backup diff reported no change to dir-purpose.txt and was wrong; `git diff` shows 5 deletions and 1 insertion. Recording the sequence rather than the outcome, because the outcome (map clean) looks identical whether the rows were removed deliberately or by a regeneration nobody inspected. Restore with `git checkout docs/dir-purpose.txt` and re-add the tools/antigravity row if the deletion is reverted upstream."
    },
    "docmap": {
      "required": true,
      "cmd": "python tools/docmap/docmap.py check",
      "_note": "Every tracked markdown file has a derivable or declared lifecycle status, and docs/DOCMAP.md matches what the documents imply. Declared 2026-07-31, six days after the tool was written: docmap ran clean the whole time and NOTHING enforced it, which is the same shape as a hook that cannot fire. A hand edit to DOCMAP.md reads as drift and fails, exactly as CODEBASE-MAP.md does, because both are generated. The residue that cannot be derived lives in docs/doc-status.txt, currently 44 rows against a stated ceiling of 37; growth past that means the derivation rules are wrong rather than that the registry should grow."
    },
    "prior_art": {
      "required": true,
      "builtin": "prior_art",
      "_note": "every component over 300 lines of Python carries an unexpired record naming what else could do its job",
      "_waiver_history": "A waiver until 2026-08-01 was added on 2026-07-31 and REMOVED the same day, unexpired, because the thing it excused was done rather than waited out. Its reason was correct when written: commit 8a4217b brought syndication-engine (549 lines), whatsapp-query (1120) and tools/intent (1016) fully into tracking with no prior-art records, and inventing three records to clear a red domain would have fabricated the evidence this domain exists to demand. All three now exist with 7, 8 and 7 fetched-and-cited alternatives and counterarguments that argue against keeping our version. Recorded here rather than deleted, because L-2026-07-31-a is a waiver renewed twice on a reason nobody executed, and the counter-practice worth keeping is that a waiver ends when its reason does."
    },
    "rules": {
      "required": true,
      "cmd": "python tools/audit/rules_sync.py check",
      "timeout": 300,
      "_note": "the global rules loaded into every session are deployed AND intact. Added 2026-08-04 after twelve of them were found at roughly a tenth of their original size, having been overwritten by commit 2bb97a8 which synced a degraded live tree back over the good repo copy and removed 986 lines. Nothing in the repo could see it: skills_sync.py covers dot-claude/skills and never mentioned rules. The shrink half of this check is the part that matters, because a byte count is the only signal left once the content is gone."
    },
    "skills": {
      "required": true,
      "cmd": "python tools/audit/skills_sync.py check",
      "timeout": 300,
      "_note": "dot-claude/skills and ~/.claude/skills agree. The checker already existed and already returned 1 on drift; what was missing was anything that ran it, so a real oracle reported problems into a terminal nobody gated on. Added 2026-08-05. The count it enforces was corrected the same day: a byte hash counted a CRLF live file and an LF repo file as drift, which inflated 55 to include 4 character-identical pairs. Those now report under LINE ENDINGS ONLY and do not fail, because no decision exists for a file whose characters match.",
      "waived": {
        "until": "2026-08-12",
        "confirm": "DRIFT: 28",
        "reason": "waived until 2026-08-12, restated 2026-08-08 at DRIFT: 28, down from the 29 recorded on 2026-08-07 and the 51 claimed on 2026-08-05. The gate itself forced this restatement: it ran the waived domain's own command, found the number had moved, reported waiver STALE and failed the tree. That is the confirm mechanism catching its author on the first change that moved the count. What moved it: i-have-adhd was DEPLOYED BUT NOT COMMITTED, the operator hit 'Skill i-have-adhd cannot be used with Skill tool due to disable-model-invocation', and 49b3570 removed that line and tracked the skill, taking that bucket from 9 to 8. Current buckets: (a) 4 COMMITTED BUT NOT DEPLOYED, so no session can invoke prod-deploy-rules, jira-task-draft, writing-great-skills or jira-read; (b) 8 DEPLOYED BUT NOT COMMITTED, all of which are tracked under dot-agents/skills, a third tree skills_sync does not read, so this bucket is as much a limit of the oracle as a state of the repo; (c) 16 SAME NAME DIFFERENT BYTES, of which 13 differ by exactly the line 'disable-model-invocation: true' that 3df7704 added to the repo copies and nobody deployed. That last group is now known to be more than cosmetic: the same line is what blocked the operator from invoking a skill at all, since /name reaches a skill through the same Skill tool the flag disables. Deploying it to 13 more skills would disable 13 more, so the correct move is probably to drop the line from the repo copies rather than push it live, and that is the operator's call. The remaining 3 real forks are shoval-voice-draft (27401b repo vs 10935b live), youtube-distill (12545b vs 8205b), and grill-me, where live is larger and the diff says the repo is AHEAD: 3df7704 rewrote it into a terse brief and live still holds the older protocol. A byte count orders files by size and says nothing about which is current. Expiry deliberately unchanged at 2026-08-12: this pass measured the waiver, it did not earn more time for it."
      }
    },
    "pointers": {
      "required": true,
      "cmd": "python tools/audit/pointers.py scan --include-live",
      "timeout": 300,
      "_note": "hooks and skills that name a path which does not resolve. Same shape as the skills row: a real checker that CI never invoked. Measured 2026-08-05 it exits 0 at fail-on=high while carrying 271 distinct absent paths, headed by ~/.claude/bin/work-item.sh at 40 references, so this row gates the high-severity class only and the long tail stays visible without blocking. No waiver, because it passes today.",
      "scope_note": "Runs with --include-live from 2026-08-07. Without it the scan read the committed dot-claude/settings.json and never ~/.claude/settings.json, so it audited a payload copy instead of the file that runs. That is not a weaker check, it is a check of a different file under the same name: the live settings had been wiring a PostToolUse hook at tools/intent/spawn_log.py, which did not exist, failing on every agent spawn, while this domain reported PASS. The payload copy has no PostToolUse section at all, so the broken hook was outside the file being read rather than merely missed. On a runner there is no live settings and the scan says so in as many words instead of passing quietly."
    }
  }
}
