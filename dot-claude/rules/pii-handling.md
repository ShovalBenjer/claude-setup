# PII handling

Minimize raw personal data in model, tool, log, and test output. Use approved
tokenization or pseudonymization before model calls, keep re-identification data
outside model context, and never mutate a production source merely to mask it.
Follow the active project's policy when it is stricter.
