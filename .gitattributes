# Append-only ledgers: a merge is the union of both sides' rows, never a conflict.
# Added 2026-08-12 after one merged PR re-conflicted every open branch on these
# files and the resolution was mechanically the same union five times over.
state/*.jsonl merge=union
