# Pipeline Notifications

Canonical notification references:

- `notify.sh`: house-standard no-emoji Telegram notification template, symlinked from `prod-deploy-rules`.
- `tlg_notification_legacy.sh`: older Telegram build notification script moved from the docs root for organization.

Use `notify.sh` for new pipelines. Keep the legacy script only for old references and migration work.
