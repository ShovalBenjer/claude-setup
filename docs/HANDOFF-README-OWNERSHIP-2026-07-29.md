# Handoff to the resume engine: the GitHub profile README is yours to own

From `new-recruit`, 2026-07-29. The operator explicitly assigned this: the
GitHub profile README is the resume engine's responsibility, not a one-off.

## Current state

`github.com/ShovalBenjer/ShovalBenjer/README.md` was **empty** (1 byte). It now
holds a **597-byte interim** that features one thing: a link to the case-ledger
post, "The Bench". Commit `5f475a2`.

That interim is deliberately minimal and says so in its own footer ("This
profile is a work in progress"). It was placed only so the profile is not blank
while the post is being syndicated. It is not the identity page.

## What the resume engine owns

The full profile README, grounded in the same evidence discipline as the resume
itself:

- **Claims need approved project or production evidence.** Studied is not
  shipped, a repo is not production. The interim states nothing about
  employment, stack proficiency, or titles for exactly this reason. Do not add
  any without the evidence the resume already requires.
- **Spoken languages are Hebrew and English.** Arabic, Spanish, Portuguese are
  product/delivery markets, not spoken ability. This trips up profile copy.
- **Do not use the contradicted `24% to 64%` metric.** `$359k` is directional
  opportunity sizing, not achieved revenue.
- Pull real, current standing from the repo ledger (`skills.json`,
  `judgment_map.json`, `research_ladder.json`) rather than inventing a bio.
- Contact details and "open to <roles>" framing are the operator's call and the
  resume engine's to place, not something to guess. The interim omits both on
  purpose (no PII added that was not requested).

## Coordination

The profile README and the CV are the same claims in two formats. Whatever the
resume engine settles as the authoritative claim set, the README is a projection
of it. Keep one source of truth; do not let the two drift.

When you replace the interim, keep or update the "The Bench" link. The post is
public, deployed, and is currently the single strongest portfolio artifact on
the profile.
